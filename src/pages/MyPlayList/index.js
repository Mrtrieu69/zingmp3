import React from 'react';
import classNames from 'classnames/bind';

import styles from './MyPlayList.module.scss';

const cx = classNames.bind(styles);

const MtPlayList = () => {
    return <div>My Playlist</div>;
};

export default MtPlayList;
