import classNames from 'classnames/bind';

import styles from './NowPlaylist.module.scss';

const cx = classNames.bind(styles);

const NowPlaylist = () => {
    return <div>Now playlist</div>;
};

export default NowPlaylist;
